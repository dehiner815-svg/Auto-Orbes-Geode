name: Build Geode Android

on:
  workflow_dispatch:

jobs:
  build:
    name: Android Build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository 📂
        uses: actions/checkout@v4

      - name: Instalar CLI 🔧
        run: |
          wget https://github.com/geode-sdk/cli/releases/download/v3.7.1/geode-cli-v3.7.1-linux.zip
          unzip geode-cli-v3.7.1-linux.zip
          chmod +x geode
          sudo mv geode /usr/local/bin/geode

      - name: Inyectar Configuración Manual 💉⚙️
        run: |
          # Crear la ruta de configuración si no existe
          mkdir -p ~/.config/geode/profiles

          # Comprobar si el archivo de perfil ya existe antes de crearlo
          if [ ! -f ~/.config/geode/profiles/miku.json ]; then
            echo '{"name": "miku", "path": "'$GITHUB_WORKSPACE'", "platform": "android"}' > ~/.config/geode/profiles/miku.json
          fi

          # Crear archivo de configuración global si no existe
          if [ ! -f ~/.config/geode/config.json ]; then
            echo '{"current-profile": "miku", "default-platform": "android"}' >
