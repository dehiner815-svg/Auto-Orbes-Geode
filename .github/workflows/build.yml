name: Build Geode Android

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - master

jobs:
  build:
    name: Android Build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository 📂
        uses: actions/checkout@v4

      - name: Build Mod (Método Docker) 🐳
        uses: addnab/docker-run-action@v3
        with:
          image: geodesdk/build-geode-mod:latest
          options: -v ${{ github.workspace }}:/work
          run: |
            cd /work
            # Aquí escribimos el comando con fuego: ¡NO PUEDE FALLAR! 👨‍🏭
            geode build --platform android --release

      - name: Upload Artifact 📤
        uses: actions/upload-artifact@v4
        with:
          name: Android-Mod-Final
          path: "**/*.geode"
