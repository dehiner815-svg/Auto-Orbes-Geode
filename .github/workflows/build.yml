name: Build Geode Android

on:
  workflow_dispatch:

jobs:
  build:
    name: Android Build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository 📂
        uses: actions/checkout@v4

      - name: Instalar y Configurar Geode 🔧
        run: |
          # 1. Descargamos el CLI 📥
          wget https://github.com/geode-sdk/cli/releases/download/v3.7.1/geode-cli-v3.7.1-linux.zip
          unzip geode-cli-v3.7.1-linux.zip
          chmod +x geode
          sudo mv geode /usr/local/bin/geode

          # 2. CONFIGURACIÓN FORZADA (Evita el pánico de Rust) 🛠️🔥
          # Creamos el perfil miku y le asignamos la plataforma de una forma que no pueda ignorar
          geode profile add --name miku .
          geode config set default-platform android
          
          # 3. Instalamos los binarios del SDK 📦
          geode sdk install-binaries

      - name: Compilar para Android 🚀
        run: |
          # 4. Compilamos usando el perfil miku 🔨
          geode build --platform android --release

      - name: Guardar Mod 📤
        uses: actions/upload-artifact@v4
        with:
          name: Mod-Android-Miku
          path: "**/*.geode"
          
