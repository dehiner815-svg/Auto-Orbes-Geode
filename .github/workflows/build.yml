name: Build Geode Android

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - master

jobs:
  build:
    name: Android Build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Build Mod 🛠️
        uses: geode-sdk/build-geode-mod@main
        with:
          # Esta es la forma correcta de decirle que es Android 📱
          target: Android
          # Forzamos la descarga de los binarios del SDK 📥
          install-binaries: true
          # Usamos la versión más compatible ⚙️
          sdk: nightly
          combine: true
          
