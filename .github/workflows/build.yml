name: Build Geode Android

on:
  workflow_dispatch:

jobs:
  build:
    name: Android Build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository 📂
        uses: actions/checkout@v4

      - name: Build Mod 🛠️
        uses: geode-sdk/build-geode-mod@main
        with:
          # Esta es la clave: le decimos el SDK y la plataforma de un solo golpe
          sdk: nightly
          target: Android
          # Forzamos la instalación de herramientas necesarias
          install-binaries: true

      - name: Guardar Mod 📤
        uses: actions/upload-artifact@v4
        with:
          name: Mod-Android-Miku
          path: "**/*.geode"
